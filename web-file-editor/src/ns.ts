/**
 * The settings namespace this plugin owns — single home shared by both
 * halves (and tests) so there is only one name to keep in sync.
 *
 * @module web-file-editor/ns
 */
export const NS = 'file-editor'
